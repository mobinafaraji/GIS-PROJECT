// vite.config.js

import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Italy_air_quality_/', // Replace with your actual repository name
  build: {
    outDir: 'dist',
  },
});
